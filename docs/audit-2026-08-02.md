# Launch-Readiness Audit — 2026-08-02

Frozen snapshot. Written against `3a936c4` (main); fixes on branch
`audit/launch-readiness-fixes-2026-08-02`. Method: launch-readiness prompt pack (12
relevant prompts) + deterministic tooling, sharded across subagents, flagship findings
verified by direct read. Supersedes the security portions of `audit-2026-07-30.md`.

## Headline

The codebase is genuinely well-built and well-tested. The single most important finding is
not a code defect: **the site is deployed and publicly reachable** (`personaudit.com` → 200,
`/api/audit` live, no auth wall) **while `docs/DEPLOYMENT.md`'s own network-isolation gate is
still open.** So the anon-reachable items below are production exposures, not pre-launch nits.

## Ground truth (tools)

- Root **291 tests pass**, web **90 pass**; `tsc` clean both; ESLint clean both; **semgrep 0**;
  **0 CVEs**, **0 gitleaks**. CVE floor met (Next 16.2.12 ≥ 16.2.6, React 19.2.8 ≥ 19.2.6;
  CVE-2025-29927 closed — auth checked in every handler). Model `claude-sonnet-5` (current).

## Findings — deduped, severity-ranked, with status

Status legend: **FIXED** (this branch) · **OPEN (infra/manual)** · **OPEN (deferred)**.

### P0 — live + exploitable
| # | Finding | Location | Status |
|---|---------|----------|--------|
| 1 | XFF anon rate-limit bypass — leftmost `x-forwarded-for` client-spoofable on Vercel → rotate it → drive public `/api/audit` to the global daily cap → all-user DoS + ~$100-250/day spend | `web/src/app/api/audit/route.ts:15`, `web/src/app/api/grade/route.ts:13` | **FIXED** — prefer `x-real-ip`, fall back to rightmost XFF |
| 2 | Live against own deploy gate — DNS-rebind egress not firewalled; Deployment Protection was OFF (07-30), still no auth wall | worker/Railway; `src/security/url-guard.ts:174-179` (documented residual) | **OPEN (infra/manual)** |

### P1
| # | Finding | Location | Status |
|---|---------|----------|--------|
| 3 | `grader_scans` SELECT `using(true)` → anyone with the public anon key enumerates every grade scan (token, URL, full report) | `web/supabase/migrations/015_grader.sql:33-36` | **FIXED** — migration `016` drops the policy; reads move to the service-role client scoped by token (`web/src/lib/grade.ts`) |
| 4 | SERIOUS a11y — scan error in a plain `<p>`, no `role="alert"` → SR users get no notice on failure/quota, on an a11y product's own public form (WCAG 4.1.3) | `web/src/components/audit-form.tsx:410`, `grade-form.tsx:114` | **FIXED** — `role="alert"` on both error regions |
| 5 | `reserveSpend` leak — reserved before the project-ownership check; "Project not found" 400 returns without `releaseSpend()` → leaks against daily cap till UTC midnight | `web/src/app/api/audit/route.ts` | **FIXED** — reservation moved after all early-return checks |
| 6 | No CSP header (other 5 security headers present) | `web/next.config.ts` | **FIXED (partial)** — `Content-Security-Policy-Report-Only` added (safe on live); enforce + report collector are the next step |
| 7 | No SPF / no DMARC on personaudit.com → auth + support email spoofable | DNS | **OPEN (infra/manual)** |
| 8 | HIGH SSRF (CLI-only): core `scan` crawler + persona-gen missing the `context.route` redirect/subresource guard their siblings have. Not a live-web exposure (hosted paths use the guarded engine) | `src/crawler/crawl.ts:48-69`, `src/personas/generator.ts:82-93` | **FIXED** — guard added to both, mirroring `grader/scan.ts`, respecting `allowPrivate` |
| 9 | No billing path — `stripe_customer_id`/`plan` scaffolded, zero Stripe code | web schema | **OPEN (deferred)** — product decision |

### P2
| Finding | Location | Status |
|---------|----------|--------|
| Global daily cap is one shared counter → one actor DoS's all users | `web/src/lib/limits.ts:20` | **OPEN (deferred)** — add a per-caller/day sub-cap under the global cap (XFF fix reduces the anon vector) |
| Waitlist insert: no rate-limit / bot protection | `web/src/app/api/waitlist/route.ts` | **OPEN (deferred)** |
| `.env.example` drift — web example omits `SUPABASE_SERVICE_ROLE_KEY` (its own code flags it as required); no `worker/.env.example` | `web/.env.local.example`, `worker/` | **OPEN (manual)** — files are permission-protected; sync by hand |
| No Sentry source-map upload → minified prod traces | `web/instrumentation*`, build | **OPEN (deferred)** |
| Tests not typechecked (`tsconfig` excludes `*.test.ts`) hid a real drift | `tsconfig.json`; drift was `src/report/generator.test.ts` | **FIXED (the bug)** — `axeFindings` moved to the correct literal (`AgentResult`, not `PersonaReport`). Systemic gap (add `tsconfig.test.json` + CI step) **OPEN (deferred)** |
| Active nav item color-only, no `aria-current` (WCAG 1.3.1) | `web/src/components/app-nav.tsx` | **FIXED** |
| Sitemap omits `/for-agencies` + `/grade` | `web/src/app/sitemap.ts` | **FIXED** |
| README "n=1" stale (task-success is n=2: Metabase + SauceDemo, both scored 0%/90%) | `README.md:24` | **FIXED** |
| Legal: no entity/governing-law/subprocessor list/DPA; manual-only deletion (GDPR Art.17/20); docs dated May 2026; no EAA accessibility statement | `web/src/app/{terms,privacy}/page.tsx`, settings | **OPEN (manual)** — content/legal work |

### P3 — nits
`/grade` missing from dogfood ROUTES → **FIXED** (`scripts/axe-dogfood.mjs`) · worker tsconfig duplicate key → **FIXED** · URL input `aria-label`-only (3.3.2) · CLI `maxSteps` unbounded → **FIXED** (`generator.ts:59`, clamped `.int().min(1).max(50)`) · timeout races-not-cancels (orphaned billable runs) `worker/src/index.ts:55` · double `auth.getUser()` waterfall (proxy+layout) · `/waitlist` not in robots · no `tunnelRoute`/analytics/OTel · em-dash overuse in marketing copy · port allowlist in url-guard · README DX (key-implied-upfront, `mpersonas` vs `pnpm dev` late, no CONTRIBUTING, lib exports undocumented). Remaining P3s **OPEN (deferred)**.

### Fixed out-of-band this session
`web/.env.local` file mode 0644 → **600**.

## What is strong (do not re-litigate)

SSRF url-guard core (resolved-IP validation, encodings, IPv6/NAT64/6to4 — 49 tests) · multi-tenant
RLS (11 tables, all scoped to `auth.uid()`/ownership, service tables deny-by-default, SECURITY
DEFINER locked, perf pattern + indexes) · atomic job claim (`SELECT FOR UPDATE SKIP LOCKED`) +
reaper + idempotent upserts · CI release gate correct (defect-key id-churn normalization, 29 tests)
· honesty claims **hard-enforced in tests** (framing, fact/opinion split, explicit finish) ·
dark-mode contrast verified 17.5:1 · reduced-motion honored · caching correct (no cross-user leak) ·
robots + llms.txt + JSON-LD + OG images present · brand voice consistent + sharp · dogfood a11y CI
gates for real (`process.exit(1)`).

## Strategy

- **Viability: NEEDS WORK / promising-but-early.** Honest, disciplined, well-built — 0 users, no
  billing, thin moat (session-crawl is copyable in a sprint).
- **Pricing rec:** per-project flat tier — Freelancer **$79/mo** (5 projects), Agency **$199/mo**
  (20 + white-label Report). Free deterministic `scan` = top-of-funnel, does not cannibalize.
- **Resourcing flag:** recent commits are persona-realism engineering — the layer README says is
  *not* the wedge. Redirect eng to scan / Report / multi-project (the validated core).
- **Cheapest 2-week de-risk:** DM ~15 agencies from the Rocketpark network the `/for-agencies` page
  + a Stripe **Payment Link** ($199 founding tier, no checkout build). 2+ pay → wedge real. Zero →
  it's the product, not the funnel.

## Gate — go / no-go

**As a public live site, as-is: NO-GO** (P0 anon cost/DoS + live against its own network-isolation
gate). Two clean paths:

1. **Restrict now** — enable Vercel Deployment Protection until the OPEN P0/P1 items close. One setting.
2. **Fix-then-stay-live** — the FIXED items above land + close the OPEN infra items (egress isolation,
   SPF/DMARC, CSP enforce). Then live is defensible.

## Manual / infra follow-ups (cannot be done from the repo)

1. **Vercel Deployment Protection** — enable for Production until P0/P1 close.
2. **Railway egress firewall** (or Browserbase-over-CDP) — closes the DNS-rebind residual.
3. **DNS** — publish SPF (include the mail sender) + DMARC (`p=none` → `p=quarantine`).
4. **Supabase Auth** — confirm email-confirmation is ON (free-tier abuse gate).
5. **`.env.example` sync** (web + worker) — permission-protected, edit by hand.
6. **Sentry** — add source-map upload (`SENTRY_AUTH_TOKEN`) or accept minified traces.
7. **Legal** — entity + governing law + subprocessor list; self-serve delete/export; EAA statement.
